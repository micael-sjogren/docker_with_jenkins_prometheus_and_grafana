#!/usr/bin/env bash

GITHUB_USER="micael-sjogren"

sudo curl -o /home/$SUDO_USER/prometheus.yml "https://github.com/$GITHUB_USER/cicd/blob/main/prometheus.yml"
sudo docker run --restart=always -d -p 9090:9090 -v /home/$SUDO_USER/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus
